# Normalize line endings to LF on checkout/commit
* text=auto eol=lf

# Shell scripts must always use LF to keep the shebang valid
*.sh text eol=lf

# Common binary types should never be normalized
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
